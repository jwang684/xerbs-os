/**
 * Per-module output schemas live here (e.g. the AssessmentModule result schema).
 *
 * Each schema implements the `Schema<T>` contract from
 * `../utils/SchemaValidator` — a Zod schema satisfies it directly. None exist
 * yet: schemas are added alongside the modules that produce them.
 */
export {};
